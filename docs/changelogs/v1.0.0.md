# v1.0.0 - SCSS Optimization & Refactoring

**Date:** December 7, 2025

## ⚡ Performance & Optimization

### 🚨 Breaking Changes
*   **Spacing Scale Unification**: Removed redundant numeric spacing classes to reduce CSS footprint and enforce consistency.
    *   **Removed**: `p-1`, `p-2`, `p-4`, `p-6`, `p-8`, etc. (and equivalently for margin/gap).
    *   **Kept**: T-Shirt sizing scale (`xs`, `sm`, `md`, `lg`, `xl`, `2xl`, `3xl`).
    *   **Migration Guide**:
        *   `*-1` → `*-xs`
        *   `*-2` → `*-sm`
        *   `*-4` → `*-md`
        *   `*-6` → `*-lg`
        *   `*-8` → `*-xl`

### 🛠️ SCSS Improvements
*   **Flexbox**: Flattened nested selectors in `_flexbox.scss`. This reduces selector specificity, making it easier to override styles if needed, and slightly reduces file size.
*   **Grid**: Consolidated responsive loop generation in `_grid.scss`. Instead of creating separate media query blocks for columns and offsets, they are now grouped, reducing the number of `@media` declarations in the output CSS.

## 📦 Distribution
*   **Size Reduction**: The minified CSS bundle (`axis-twelve.min.css`) is now approximately **40KB** (6KB gzipped), significantly smaller due to the removal of duplicate spacing definitions.
