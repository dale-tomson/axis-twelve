# NPM Publishing Configuration
# Copy this file to .env and add your actual npm token

# Your npm authentication token from https://www.npmjs.com/settings/~/tokens
# Generate a token with "Automation" scope for CI/CD environments
NPM_TOKEN=your_npm_token_here

# Optional: NPM registry URL (defaults to https://registry.npmjs.org/)
# NPM_REGISTRY=https://registry.npmjs.org/

# Optional: Package scope (if using scoped packages like @yourname/axis-twelve)
# NPM_SCOPE=@yourname
